# Conexão direta com o Postgres (mesmo banco usado pelo back-gestao),
# usada pelas Server Actions do catálogo (lib/db.ts)
PG_USER="user"
PG_HOST="host"
PG_DATABASE="database"
PG_PASSWORD="password"
PG_PORT=5432

# Número de WhatsApp da loja (com DDI+DDD, só dígitos) usado nos botões de contato
NEXT_PUBLIC_WHATSAPP_NUMBER="5513997544393"
